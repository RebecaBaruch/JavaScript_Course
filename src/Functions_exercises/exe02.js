//Writea function that takes two numbers and returns the maximum of the two
let number = max (1, 2);
console.log(number);

function max(num1, num2) {
    // if(num1 > num2) return num1;
    // return num2; 

    return (num1 > num2) ? num1 : num2;
}